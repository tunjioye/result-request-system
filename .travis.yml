language: php

php:
  - 5.6
  - 7.3

addons:
  postgresql: "12.1"

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script:
  - ./vendor/bin/phpunit
